1.List接口
        ------ 有序、有下标、可以重复
            ------ ArrayList 本质是数组，连续的内存空间。查找速度快、增删慢
            ------ LinkedList 内部实现是一个双向的链表。查找慢，增删快

2.Set接口
        ------ 无序、无下标、不可重复
            ------ HashSet 存储结构是 哈希表(数组+链表)   jdk1.8后变成 数组+链表+红黑树
                   理解：可以类比银行多个窗口排队的情况
                   银行有多个服务窗口，例如窗口1、窗口2、窗口3、窗口4、窗口5等，多个窗口就好比是数组
                   每个窗口后面都有很多人排队，这个就好比是链表
                   存储过程：
                   1.根据hashCode()计算保存的位置。如果当前位置为空，即保存在该位置上。如果当前位置不为空，则执行步骤2.
                   2.执行equals()方法进行比较，如果相同，则不保存。如果不同，则形成链表。

            ------ TreeSet 存储结构是红黑树
                   1.基于排序算法是实现的
                   2.实现了SortedSet接口
                   3.必须是可以比较的 comparetor  comparable

